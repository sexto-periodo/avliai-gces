class Subject {
  double id;
  String hashId;
  String name;
  String course;
  String campus;
  String shortDesc;
  String picUrl;
  double grade;

  Subject(this.id, this.hashId, this.name, this.course,this.campus, this.shortDesc, this.picUrl, this.grade);
}